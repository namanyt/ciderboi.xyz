---
description: Build or refresh the repo-local Graphify graph and wire graphify-local into opencode.json
---
Run the shell command `!./.opencode/scripts/build-graphify` and then summarize whether `graphify-out/graph.json` and `graphify-local` were updated successfully.
